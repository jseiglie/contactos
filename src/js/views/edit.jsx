import React, { useEffect } from "react";
import { EditUserInfo } from "../component/editUserInfo.jsx";

export const Edit = () => {

    //vista edicion, renderiza el componente de edicion

    return (
        <div className="container">
            <EditUserInfo />
        </div>
    )
}